# Copy to .env.local. NEXT_PUBLIC_ vars are inlined into the browser bundle at build time,
# so nothing secret belongs here — the admin panel holds no credentials of its own.
NEXT_PUBLIC_API_URL=http://localhost:4000
